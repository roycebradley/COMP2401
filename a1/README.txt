/*
Preamble:

Royce Bradley

A program that takes in an integer and converts it to the specified base.

Source Files:
convert.c
convert.h
main.c

To call the convert() function you must provide two arguments. The first is an int that is the number to convert and the second is an int that specifies which base to convert to. convert(int number, int base).

To launch compile main.c and convert.c using the command "gcc main.c convert.c".
Then launch ./a.out to run the main function.The program is now complete.
*/
